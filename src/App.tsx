import { BrowserRouter } from "react-router-dom";
import AllRoute from "./components/routePage";

export default function App() {
  return (
    <BrowserRouter>
      <AllRoute />
    </BrowserRouter>
  );
}
